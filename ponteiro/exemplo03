 #include <stdio.h>
 
 int main(){
 	int vetor[5] = {1,2,3,4,7}; // Um int = 4 bytes de memoria .... entao 
 	int *p = vetor;
 	int i;
 	
 	for(i = 0; i < 5; i++){
 		printf("Elemento %d: %d\n",i,*(p+i));	
	}
 	
 	return 0;
 }
